1. Phần Logic & UI (page.tsx)Xóa fillDemo và JSX: Chỗ này chuẩn xác. Hàm fillDemo sinh ra chỉ để gán state cứng cho 2 nút demo. Việc gỡ nó và cái div chứa 2 nút ở dưới form đi là dứt điểm được cái UI.  Giữ luồng chính: Việc khoanh vùng rõ là giữ nguyên handleSubmit với mấy cái state username, password sẽ giúp form login chính hoạt động bình thường, không suy suyển gì. Bạn nhớ check lướt qua xem sau khi xóa fillDemo, có import nào (kiểu như icon hay type gì đó dành riêng cho 2 nút kia) bị dư ra không thì clean luôn cho file nó sạch nhé.  2. Dọn dẹp I18n (vi.json & en.json)Đánh giá cực cao vụ bạn cẩn thận note lại việc "KHÔNG XÓA" key "staff" ở L425 thuộc block nav! Làm file translation rất hay bị dính cái lỗi replace all hoặc xóa nhầm mấy key trùng tên ở các module khác nhau. Xóa đúng 3 keys trong block auth ở cả 2 file en và vi là an toàn nhất rồi.  3. Kế hoạch VerificationE2E Tests: Đúng là script test auto hiện tại thường dùng hành vi nhập thật bằng hàm fill kết hợp click("Đăng nhập") thay vì nhấn nút demo. Việc xóa nút demo đi thì script cũ vẫn pass ngon ơ. Chạy thử cái npm run build để check type cũng là một thói quen tốt để tránh lỗi vặt khi build project.  Manual Test: 5 bước test tay của bạn đã cover đủ cả happy path (login thành công trỏ về /dashboard), unhappy path (sai pass báo lỗi) và test đổi ngôn ngữ i18n. Thế này là quá đủ để chốt hạ PR rồi.  