"use client";

/**
 * @deprecated This component is no longer used. We switched to Sonner for notifications.
 * The implementation is kept empty to avoid type errors in other files that might still import it.
 */
export function ToastContainer() {
  return null;
}
